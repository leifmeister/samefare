[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'"
preDeployCommand = "/opt/venv/bin/python scripts/migrate.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
